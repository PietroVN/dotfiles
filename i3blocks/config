[clock]
command=echo "$(date +%R)"
interval=60

[date]
command=echo "$(date +%e-%m-%Y)"
interval=900

[battery]
command=echo "$(cat /sys/class/power_supply/BAT0/capacity)%"
interval=300

#[wifi]
#command=echo "$(iwctl station wlan0 show | awk '/Connected network/{print $3,$4}')"
#interval=300

#[speed]
#command=echo "$(iwctl station wlan0 show | grep TxBitrate |awk '{print $2}' | tail -n3) | $(iwctl station wlan0 show | grep RxBitrate |awk '{print $2}' | tail -n3 )"
#interval=1

